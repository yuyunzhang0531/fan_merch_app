window.FAN_MERCH_CONFIG = {
    // Render 一体部署默认走同域后端，不需要单独配置。
    apiBase: window.location.origin,
    // 模板和贴纸默认走 CDN，提升静态素材首屏读取速度。
    assetBase: 'https://cdn.jsdelivr.net/gh/yuyunzhang0531/fan_merch_app'
};